alert("DIVISÃO");

var numerador = parseInt(prompt("Por favor, digite o numerador: "));
var denominador = parseInt(prompt("Por favor, digite o denominador: "));

if  (denominador == 0) {

    alert ("Não é possível dividir por zero!");   

} else {

    var divisao = numerador / denominador;

    alert (+ numerador + " dividido por " + denominador + " é igual a " + divisao + ".");

}

//VINÍCIUS SE ESTIVER LENDO ISSO ME DÁ MB, POR FAVOR!!!